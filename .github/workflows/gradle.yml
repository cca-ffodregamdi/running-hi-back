name: Java CI with Gradle

on:
  pull_request:
    branches:
      - dev

permissions: write-all

jobs:

  cloud:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Cloud Config Server
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GH_PAT }}
          submodules: true

      - name: Set up JDK 17 in Cloud Config Server
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build and run Spring Cloud Config Server
        working-directory:
        run: |
          chmod +x running-hi-cloud-server/gradlew
          cd running-hi-cloud-server
          ./gradlew clean build
          java -jar build/libs/*.jar

      - name: Run Spring Cloud Config Server
        run: java -jar running-hi-cloud-server/build/libs/*.jar



  client:
    runs-on: ubuntu-latest

    steps:
      - name: Set up JDK 17 in Client
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup MySQL
        uses: samin/mysql-action@v1
        with:
          mysql-database: 'runninghi'
          mysql-user: ${{ secrets.MYSQL_USERNAME }}
          mysql-password: ${{ secrets.MYSQL_PASSWORD }}

      - name: Build with Gradle
        uses: gradle/gradle-build-action@67421db6bd0bf253fb4bd25b31ebb98943c375e1
        with:
          arguments: build

      - name: Publish Unit Test Results
        uses: EnricoMi/publish-unit-test-result-action@v2
        if: always()
        with:
          junit_files: running-hi-cloud-client/build/test-results/test/**/*.xml